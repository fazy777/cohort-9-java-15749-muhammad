{
  "name": "frontend-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "contact_managment_system"
  ],
  "scripts": {
    "build": "npm --prefix contact_managment_system run build",
    "dev": "npm --prefix contact_managment_system run dev"
  }
}
